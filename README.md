# Wisconsin Vehicle Recovery
Mobile-first decision-support build onboarded through Routalk Pilot.

North star: **Complicated underneath. Simple on top.**

Architecture: **CASE STATE → RELEASE PATH → ENFORCEMENT PATH → CAR**.

ASK PILOT is contextual assistance above the deterministic engine and cannot independently override engine decisions.

Run the core regression suite with:

`node tests.js`
